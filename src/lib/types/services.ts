// src/lib/types/services.ts

export interface Service {
    id: string;
    title: string;
    description: string;
    icon: string; // This can be an emoji, a path to an SVG, or a class name for an icon font
  }